# /start

Read `PROJECT.md` end-to-end. Restate the current task in one sentence.

Then read the latest files under `docs/specs/` and `docs/plans/` if they exist. Check `git status` and `ccc status` if `ccc` is installed. Confirm which workflow should be used next.
